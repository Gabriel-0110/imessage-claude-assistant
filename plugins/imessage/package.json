{
  "name": "imessage",
  "version": "0.8.0",
  "description": "Local iMessage MCP channel server for Claude Code.",
  "license": "Apache-2.0",
  "author": "Gabriel Chiappa",
  "repository": {
    "type": "git",
    "url": "https://github.com/gabrielchiappa/imessage-claude-assistant.git"
  },
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --silent --frozen-lockfile >/dev/null 2>&1 || bun install --silent >/dev/null 2>&1; exec bun server.ts",
    "setup": "bun install"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  }
}
